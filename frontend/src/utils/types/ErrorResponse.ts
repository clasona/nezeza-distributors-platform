// utils/types/ErrorResponse.ts (or similar)
export interface ErrorResponse {
  msg?: string; // Or msg, depending on your API
  status?: number;
  // Add other properties if your API includes them
}
